#!/bin/bash
# QuickPDF.move.selected.down.sh - Move the selected file one place down the list,
# which is the order Merge combines the files in

source "${OMC_APP_BUNDLE_PATH}/Contents/Resources/Scripts/lib.QuickPDF.sh"

move_selected_file down
