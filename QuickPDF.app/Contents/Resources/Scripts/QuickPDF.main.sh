#!/bin/bash
# QuickPDF.main.sh - Entry point for QuickPDF applet
#
# The window is opened by NEXT_COMMAND_ID = QuickPDF.new; the object context
# (files dropped on the app icon) propagates to the chained command, and
# QuickPDF.init seeds the file list from OMC_OBJ_PATH.
