#!/bin/bash
# QuickPDF.main.sh - Entry point for QuickPDF applet

# The ActionUI window is shown via the ACTIONUI_WINDOW definition in
# Command.plist. The INIT_SUBCOMMAND_ID (QuickPDF.init) runs automatically.
